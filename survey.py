import os
import numpy as np
import pandas as pd

dir = './data/dataset0'


def get_length_from_path(self, a):
    l1_start = 0
    l1_end = len(pd.read_csv(a).to_numpy())
    return [l1_start, l1_end]


def
os.listdir(dir)
